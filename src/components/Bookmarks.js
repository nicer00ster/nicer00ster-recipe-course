import React from 'react';
import Recipe from './Recipe';

const Bookmarks = props => {
  // let eachBookmark;
  // eachBookmark = props.bookmarkKeys.map(bookmark => {
  //   console.log(bookmark);
  // })
  // eachBookmark = props.bookmarks[`${props.bookmarkKeys}`].map((item, key) => {
  //   return (
  //     <Recipe key={key} uid={props.uid} recipe={item} />
  //   )
  // })
  return (
    <div>
      {eachBookmark}
    </div>
  )
}

export default Bookmarks;
